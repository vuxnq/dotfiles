if status is-interactive
    # Commands to run in interactive sessions can go here
end

set -Ux FZF_DEFAULT_OPTS "\
--color=bg+:#{{ .theme.colors.s_0 }},bg:#{{ .theme.colors.bg }},spinner:#{{ .theme.colors.a_rose }},hl:#{{ .theme.colors.a_red }} \
--color=fg:#{{ .theme.colors.fg }},header:#{{ .theme.colors.a_red }},info:#{{ .theme.colors.a_mauve }},pointer:#{{ .theme.colors.a_rose }} \
--color=marker:#{{ .theme.colors.a_rose }},fg+:#{{ .theme.colors.fg }},prompt:#{{ .theme.colors.a_mauve }},hl+:#{{ .theme.colors.a_red }}"

set -gx EDITOR nvim

set -gx PATH $PATH ~/.local/bin
set -gx PATH $PATH ~/.spicetify # spicetify
set -gx PATH $PATH ~/.cargo/bin # rust cargo
set -gx PATH $PATH ~/.filen-cli/bin # filen-cli

zoxide init --cmd cd fish | source

fish_add_path /home/vuxnq/.spicetify

# opencode
fish_add_path /home/vuxnq/.opencode/bin
