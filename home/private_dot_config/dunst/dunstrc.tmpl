[global]
    follow = keyboard
    format = "<b>%s</b>\n%b"

    icon_path = "~/.local/share/icons/"
    max_icon_size = 64
    enable_recursive_icon_lookup = true

    origin = top-right
    offset = 10x10
    frame_width = 2
    frame_color = "#{{ .theme.colors.s_0 }}"
    separator_color= frame
    font = "{{ .theme.font.name }} Medium"
    corner_radius = 2
    background = "#{{ .theme.colors.bg_darkest }}"
    foreground = "#{{ .theme.colors.fg }}"

    progress_bar_corner_radius = 2
    
[urgency_low]
    foreground = "#{{ .theme.colors.fg }}"
    highlight = "#{{ .theme.colors.a_lavender }}"
    timeout = 4

[urgency_normal]
    foreground = "#{{ .theme.colors.fg }}"
    highlight = "#{{ .theme.colors.a_lavender }}"
    timeout = 6

[urgency_critical]
    foreground = "#{{ .theme.colors.fg }}"
    highlight = "#{{ .theme.colors.a_lavender }}"
    timeout = 0
